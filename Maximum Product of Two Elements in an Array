class Solution:
    def maxProduct(self, nums: List[int]) -> int:
        a=[]
        for i in range(len(nums)):
            for j in range(len(nums)):
                if i!=j:
                    b=(nums[i]-1)*(nums[j]-1)
                    a.append(b)
        return max(a)
                
                
        
